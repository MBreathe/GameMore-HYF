function Suggestions({ results }: { results: string[] }) {
  /*
    const filteredData = data.filter((el) => {
        input === ''
            ? el
            : el.text.toLowerCase().includes(input)



    return (
        <ul>
            {filteredData.map((el: string) => <li>{el.text}</li>)}
        </ul>
    )
    */
  return null;
}

export default Suggestions;
